#!/bin/bash

# Add here env variables needed to build production images

export APPVERSION=v0.0.1
export NODEVERSION=10.9.0
export IMAGENAME=boilerplate-webpack-barebone-angular-frontend-production


# GitLab credentials for push
# Created 2018-07-11, valid up to ~2018-10-11

export GITLAB_USERNAME=XXX
export GITLAB_REGISTRY_TOKEN=XXX
